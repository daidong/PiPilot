/**
 * Sandbox Provider Interface
 *
 * Abstraction over execution environments. Process sandbox (v1.0) and Docker (v2.0)
 * both implement SandboxProvider.
 */

export type { SandboxProvider, SandboxHandle, SpawnConfig } from '../types.js'
